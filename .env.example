# Copia este archivo como .env y reemplaza con la cadena del Session pooler de Supabase
# (Supabase → Connect → Session pooler). En Render, configura DATABASE_URL en Environment.
DATABASE_URL=postgresql://postgres.<ref-proyecto>:<contraseña>@aws-0-<region>.pooler.supabase.com:5432/postgres?sslmode=require
